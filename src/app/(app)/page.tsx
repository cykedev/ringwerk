// Dashboard — Übersicht aktiver Ligen.
// Wird als erstes nach dem Login angezeigt.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-muted-foreground">Aktive Ligen werden hier angezeigt.</p>
    </div>
  )
}
