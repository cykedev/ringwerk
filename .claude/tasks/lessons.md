# Lernlog – Liga-App

Wird nach jeder Nutzerkorrektur aktualisiert.
Format: Datum | Fehler | Regel die ihn verhindert

---

| 2026-03-09 | Punktevergabe mit 3 statt 2 dokumentiert; Unentschieden-Regelung fehlte komplett | Vor dem Dokumentieren von Spielregeln explizit nachfragen statt SRS-Defaults übernehmen |
| 2026-03-09 | Projekt-Setup: `postcss.config.mjs` vergessen → `shadcn/tailwind.css` und `tw-animate-css` nicht auflösbar | Bei Tailwind 4 + shadcn immer `postcss.config.mjs` mit `@tailwindcss/postcss` anlegen — ohne diese Datei schlägt die CSS-Auflösung im Dev-Server fehl |
| 2026-03-09 | DB-Port 5432 bei parallelen Projekten belegt → Docker `up` schlägt fehl | In `docker-compose.dev.yml` den extern gemappten Port projektspezifisch wählen (1gegen1: 5433, treffsicher: 5432) |
| 2026-03-09 | `z.string().optional()` akzeptiert kein `null` — `FormData.get()` liefert `null` für fehlende Felder | Optionale FormData-Felder immer mit `z.string().nullable().optional()` validieren; `parseDeadline` mit `string \| null \| undefined` typisieren |
| 2026-03-09 | Prettier beanstandete lange Zeilen in neu generierten Dateien (gleicher Fehler zweimal) | Nach jeder neuen Datei lokal `npx prettier --write <datei>` ausführen, bevor der Docker-Check läuft — spart einen Round-Trip |
| 2026-03-09 | `prompt()` gibt `null` zurück wenn Nutzer abbricht; `?? ""` würde `null` in `""` wandeln und Cancel ignorieren | Bei `prompt()`-Rückgabewert zuerst `if (result === null) return` prüfen, dann erst den Wert verwenden |
| 2026-03-09 | Prisma-Relation `leagueParticipants` im Nested-Select verwendet, obwohl das Feld im Schema `leagues LeagueParticipant[]` heisst → `PrismaClientValidationError: Unknown field 'leagueParticipants'` | Vor jedem Nested-Select den exakten Relationsnamen aus `schema.prisma` ablesen — der Feldname auf dem Model (nicht der Typ) ist der korrekte Select-Key |
| 2026-03-10 | CSS-Grid mit `sm:grid-cols-2` + `max-w-sm` auf einem Grid mit nur 1 Item: Das Item landet in Spalte 1 und bekommt nur (max-w − gap) / 2 ≈ 184 px statt der erwarteten vollen Breite | `sm:grid-cols-2` und `max-w-*`-Constraints immer entweder/oder auf demselben Grid-Container anwenden — bei single-item-Grids ausschliesslich `max-w-* mx-auto`, niemals gleichzeitig mehrere Spalten |
| 2026-03-10 | shadcn/ui Card hat ab Werk `px-6` auf `CardHeader` und `CardContent` — auf Mobile (375 px) bleibt wenig Nutzfläche | Card-Subkomponenten-Padding beim Einsatz immer explizit mit `px-4 sm:px-6` überschreiben; das Default-`px-6` ist für Desktop-Layouts, nicht für Mobile-First |
| 2026-03-10 | Icon-only-Button-Muster: `<Plus className="mr-1 h-3 w-3" /> Eintragen` zeigt Text auch auf Mobile → verengt verfügbare Zeilenbreite | Responsive Button-Muster: `px-1.5 sm:px-2` + `<Icon className="h-3 w-3 sm:mr-1" />` + `<span className="hidden sm:inline">Label</span>` — identisch zum Nav-Button-Pattern |
| 2026-03-10 | `preview_start` schlägt fehl wenn Docker Compose bereits manuell auf Port 3000 läuft — Stop-Hook feuert bei jeder Code-Änderung | Docker immer über `preview_start` starten (einmal `docker compose down`, dann `preview_start`) — nie manuell, damit die Registrierung erhalten bleibt. Registrierung überlebt keine Session-Grenze: zu Sessionbeginn prüfen ob `preview_list` leer ist |
| 2026-03-10 | `table` ohne `overflow-x-auto` auf Mobile: Spalten werden abgeschnitten statt gescrollt — Nutzer sieht stumm unvollständige Tabelle | Mobile-first bei Tabellen: weniger wichtige Spalten mit `hidden sm:table-cell` ausblenden statt auf horizontales Scrollen zu setzen; Padding `px-4` → `px-2 sm:px-4` für enge Viewports |
| 2026-03-10 | Touch-Targets mit `h-8 w-8` (32px) zu klein für iPhone — Buttons schwer tippbar | Icon-Buttons in Listen immer mindestens `h-10 w-10` (40px); Apple empfiehlt 44px. `h-8` nur für Desktop-only-UIs akzeptabel |
| 2026-03-10 | Native `prompt()` / `confirm()` in async-Callbacks werden von iOS Safari teilweise geblockt | Immer shadcn/ui `Dialog` (mit Formularfeldern) oder `AlertDialog` (Bestätigung) verwenden — kein `prompt()`/`confirm()`/`alert()` in Server-Action-Callbacks |
| 2026-03-10 | Nacktes SVG-Icon (`<Clock />`) zentriert nicht zuverlässig per `text-center` auf `<td>` | SVG-Icons in `<span className="inline-flex items-center justify-center">` wrappen wenn sie als einziger Zelleninhalt stehen |
| 2026-03-10 | Kaskadenlöschung ohne `onDelete: Cascade` im Prisma-Schema: Einfach `deleteMany` auf der Parent-Tabelle schlägt fehl wegen FK-Constraints | Manuelle bottom-up-Reihenfolge in einer Transaktion: zuerst die tiefsten Kindtabellen löschen (z.B. PlayoffDuelResults), dann aufwärts bis zum Root-Objekt (League). Reihenfolge aus dem Prisma-Schema ableiten: Relation → FK → welche Tabelle hält den FK → die muss zuerst geleert werden |
| 2026-03-11 | `export type { Foo }` in einem `"use server"`-Modul verursacht Turbopack Build-Error: "Export Foo doesn't exist in target module" — TSC findet den Fehler nicht | Typen niemals aus `"use server"`-Dateien re-exportieren. Typen in eine eigene `types.ts` auslagern und von dort direkt importieren — in Komponenten `import type { Foo } from "@/lib/foo/types"`, nicht `from "@/lib/foo/actions"` |
| 2026-03-11 | `UserRowActions` nutzte Dropdown (`MoreHorizontal`) statt Inline-Buttons — inkonsistent zu `ParticipantRowActions` und `LeagueParticipantActions` | Alle Zeilenaktionen in Listen: Inline-Icon-Button-Pattern (`variant="ghost" size="icon" className="h-10 w-10"`), kein `DropdownMenu` — Aktionen müssen direkt sichtbar sein, nie hinter `...` versteckt. Ausnahme: Ligen-Karten-Aktionen mit vielen Status-abhängigen Optionen |
| 2026-03-11 | Nutzerliste trennte aktiv/inaktiv nicht visuell — nur ein `Inaktiv`-Badge; Teilnehmer-Liste hatte die Trennung bereits korrekt | Listen mit aktiv/inaktiv-Status immer in zwei Sektionen aufteilen: aktiv oben normal, inaktiv unten mit Label + `opacity-60` + `line-through` — analog `participants/page.tsx` als Referenz |
| 2026-03-11 | Listen-Container (`rounded-lg border`) ohne `bg-card` wirken im Dark Mode schwarz und vom Seitenhintergrund nicht unterscheidbar | Alle Listen- und Tabellen-Container: `rounded-lg border bg-card` (Listen) bzw. `overflow-hidden rounded-lg border bg-card` (Tabellen) — `bg-card` ist Pflicht |
| 2026-03-11 | Void-Matches im Spielplan zeigten Ergebnis und grünen Hintergrund für die nicht-zurückgezogene Seite | `isVoid`-Flag auf Match-Ebene auswerten (`homeWithdrawn \|\| awayWithdrawn`), an `ParticipantResult` weitergeben: beide Namen `line-through`, kein Ergebnis, kein `bg-emerald-500/10` auf Zellen |
| 2026-03-11 | Prisma `migrate dev` schlägt fehl mit "drift detected" wenn Schema-Änderungen zuvor per `db push` direkt in die DB geschrieben wurden, ohne Migration | `db push` nie für Produktions- oder Dev-Persistenz nutzen — nur für schnelle Exploration. Wenn Drift auftritt: `prisma migrate reset --force` (verliert alle Daten), dann `migrate dev` neu aufsetzen |
| 2026-03-11 | Prisma-Relation-Löschstrategie `onDelete: SetNull` auf optionalem FK — funktioniert nur wenn das Feld nullable ist (`String?`); bei `String` würde die FK-Constraint-Verletzung trotzdem auftreten | Optionale Relationen (`leagueId String?`) mit `onDelete: SetNull` kombinieren; Pflicht-Relationen mit `onDelete: Cascade` oder manueller Kaskade. Nie `onDelete: SetNull` auf `String` (non-nullable) |
| 2026-03-11 | AuditLog-Einträge mit `{ matchupId }` als Detail waren nutzlos — kein Kontext ohne DB-Join | Audit-Details immer denormalisiert schreiben: Namen, Runden, Werte zum Zeitpunkt des Events direkt in `details` JSON speichern — auch wenn das Objekt noch existiert. Details-JSON ist ein Snapshot, kein Verweis |
| 2026-03-11 | Protokoll-Link in jeden Sub-Page-Header eingebaut → Header zu voll, Nutzer-Feedback "zu breit" | Seltene Admin-Funktionen (Protokoll, Gefahrenzone) nicht in Navigation-Header — stattdessen in `...`-Dropdown (LeagueActions) oder eigene Sektion. Header-Icons nur für häufig genutzte Kernnavigation |
| 2026-03-11 | `PdfDownloadButton` hatte `size="sm"` mit sichtbarem Label — inkonsistent zu allen anderen Header-Buttons die `size="icon"` nutzen | Alle Buttons im Liga-Header: einheitlich `variant="outline" size="icon" className="h-9 w-9"` — Text nur in Hauptaktions-Buttons (z.B. CTA-Buttons im Body), nie in Header-Navigation |
| 2026-03-12 | Prisma-Migration musste manuell erstellt werden (Rename Column ist nicht-interaktiv) — mit Docker-Runner `prisma migrate dev` nicht möglich | Bei nicht-interaktiven Migrationen (Rename, Drop Column etc.) die Migration manuell in `.prisma/migrations/<timestamp>_<name>/migration.sql` schreiben, danach lokal `prisma migrate dev` aufrufen oder die `.env` in Docker-Kontext kopieren |
| 2026-03-12 | Beim Rename `email` → `contact` wurden `leagueParticipants/types.ts` und `leagueParticipants/queries.ts` übersehen — Participant-Email wird auch im nested select der Liga abgerufen | Beim Umbenennen von Fields: nicht nur direkte Nutzung durchsuchen, sondern auch Nested Selects in verwandten Queries prüfen — `Participant` kann in vielen Kontexten genested sein (League, Matchup, Result etc.) |
| 2026-03-16 | Prisma Decimal-Feld mit `@default(1.0)` auf neue Spalte in bestehendem Model — neue Werte erhalten automatisch 1.0, aber alte Zeilen zeigen NULL statt Default-Wert | Prisma-Defaults mit `@default(...)` gelten nur für neue Datensätze. Bei Migrations auf bestehendem Model: Migrationsstrategie immer vorher klären — nullable mit manueller Nachverarbeitung vs. non-nullable mit `data` Block in Migration vs. `@default` mit anschliessender Update-Query |
| 2026-03-16 | System-Disziplinen nur with find+skip auslesen/aktualisieren wäre nicht idempotent — neue Spalten auf bestehenden Zeilen würden nicht aktualisiert bei Wiederholung | `upsert` Pattern für Startup-Daten: `where` mit deterministischem Schlüssel, `create` für Neuanlage mit vollständigen Feldern, `update` mit nur den Feldern die sich ändern können — idempotent, da Updates nur bei Bedarf feuern |
| 2026-03-16 | Nach Schema-Änderung (neue Spalte auf Discipline) war Typecheck fehlgeschlagen mit ungültiger Typ — Docker-generierter Prisma-Client war veraltet | Nach jedem `prisma migrate deploy` oder `db push`: lokal `npx prisma generate` ausführen, BEVOR Typecheck läuft. Docker-Client-Cache kann veraltet sein; lokale Regeneration ist schneller als auf Docker-Container zu warten. Client unter `src/generated/prisma/` ist die Quelle für alle Typen |
| 2026-03-16 | PostgreSQL konnte beim Enum-Typ-Änderung die Spalten-DEFAULT nicht auto-aktualisieren — Migration fiel bei `ALTER TYPE` fehl wegen bestehender `DEFAULT`-Klauseln | Enum-Typ-Änderung in drei Schritten: (1) `ALTER TABLE ... ALTER COLUMN ... DROP DEFAULT`, (2) `ALTER TYPE ... ADD VALUE ...` und/oder `ALTER COLUMN ... SET DATA TYPE ...`, (3) `ALTER TABLE ... ALTER COLUMN ... SET DEFAULT ...` — Defaults müssen vor Typ-Änderungen weg, danach wieder rein |
| 2026-03-16 | Nach manueller Migration wurde Prisma `migrate dev` mit neuer CURRENT-Timestamp aufgerufen — die auto-generierte Migration bekam einen Zeitstempel kleiner als die manuell geschriebene, verursachte falsche Anwendungsreihenfolge auf Fresh-Install | Manuelle Migrationen mit future-Timestamp anlegen (z.B. `20260317_000000_*`), danach die auto-generierte Migration mit dem kleineren Timestamp sofort löschen und die Manual-Migration um die restliche Diff ergänzen — so bleibt die Reihenfolge konsistent |
| 2026-03-16 | Beim Umbenennen von 57 Dateien über mehrere Layer: Page-Level-Props wurden nicht erfasst — sie referenzieren Component-Interface-Namen und wurden von Parallel-Layer-Agents übersehen | Grosse Refactorings mit Parallel-Agents: Pro Agent einen Layer (lib, components, routes, API) — danach zusätzlich einen Pass für Page-Props, die vom Component-Rename abhängen. Props-Update läuft nach allen anderen Layer-Passes, nicht parallel |
| 2026-03-16 | Test-Suite fiel fehl wegen alten Git-Worktrees mit veralteten Import-Paths — Tests fanden die alten `.ts` noch auf der Disk obwohl der Code refaktoriert war | Vor `npm test` immer `rm -rf .claude/worktrees/*` ausführen — alte Session-Worktrees können Test-Imports vergiften. Cleanup-Step: obligatorisch vor Testing nach Multi-Session-Work |
| 2026-03-17 | PostgreSQL: `CREATE UNIQUE INDEX` (Prisma-Default) mit `ALTER TABLE ... RENAME CONSTRAINT` umbenennen schlug fehl — Syntax für Indexes falsch angewendet | Unique Indexes (Prisma) mit `ALTER INDEX ... RENAME TO` umbenennen. Nur Constraints (von `ADD CONSTRAINT` erstellt) können `ALTER TABLE ... RENAME CONSTRAINT` nutzen — INDEX und CONSTRAINT sind unterschiedliche DDL-Objekte |
| 2026-03-17 | `teilerFaktor` auf Discipline existierte seit Phase 1, wurde aber nie in Ringteiler-Berechnungen multipliziert — Faktor lag ungenutzt | Neue numerische Felder sofort in alle relevanten Rechner integrieren. Bei Phase 3 mussten results/actions.ts und playoffs/actions.ts `teilerFaktor` fetchen und durchreichen |
| 2026-03-17 | Prisma-Relation umbenannt (Matchup.results → Matchup.series), aber Prisma-Select-Queries nicht aktualisiert — `select: { results }` schlägt fehl | Beim Rename einer Prisma-Relation: ALL Prisma select/include-Queries auf den neuen Relationsnamen aktualisieren. Application-Interface-Felder (z.B. MatchupListItem.results) können sich unterscheiden — nur der Prisma-Query-Key muss korrekt sein |
| 2026-03-17 | Phase-2-Test-Mocks nach League→Competition-Rename nicht vollständig aktualisiert — Mocks verwendeten `league: { count: mock }` statt `competition: { count: mock }` | Test-Mocks nach Prisma-Renames auch in Mock-Objekten aktualisieren, nicht nur im Production-Code — `count`-Mocks mit alten Feldnamen verursachen kaskadierte Fehler in Listen-Tests |
| 2026-03-17 | Series-Model benötigte neues `competitionId String?`-Feld — EVENT-Series (matchupId=null) konnten sonst nicht zu Competition verlinkt werden | Neue relationale Entity-Felder vor Core-Logic-Implementierung definieren; danach erst die Queries + Actions anpassen — umgekehrte Reihenfolge zwingt zu Manual-Migrations |
| 2026-03-17 | Zod `.pipe(z.number().nullable().optional())` verursachte Type-Inkompatibilität bei Teiler-Berechnung — `.transform()` alleine reichte aus | Optionale Zahlen-Felder: nur `.transform()` benutzen, kein `.pipe()` mit Typ-Casting — `.pipe()` kommt nach `.transform()` und kann zu Typ-Kollisionen führen |
| 2026-03-17 | `createCompetition` Action musste `ActionResult<T>` Generic zurückgeben statt nur `void` — nur so konnten die neuen `{ id }` für Redirect mitgegeben werden | Server-Actions mit Rückgabewert immer TypeScript-generisch typisieren: `ActionResult<{ id: string }>` statt implizit `void`. Frontend erwartet Daten für Redirect/State-Update |
| 2026-03-17 | `calculateRingteiler` mit falscher Parameterreihenfolge führte zu stillen Bugs — korrekte Reihenfolge ist `(rings, teiler, teilerFaktor, maxRings)` | Numerische Kalkulationsfunktionen: Parameterreihenfolge in JSDoc + Tests dokumentieren; False-Positives durch Zahlen-Ähnlichkeit sind hard to debug |
| 2026-03-17 | shadcn Checkbox-Komponente verlangte explizite Typ-Annotation `(checked: boolean \| "indeterminate")` auf onCheckedChange — implizit erzeugte Type-Mismatch | shadcn-UI-Komponenten mit generischen State-Handlern immer mit vollständiger Typ-Signatur binden; Auto-Inference schlägt bei Conditional-Types fehl |
| 2026-03-17 | `discipline.abbreviation`-Feld nicht auf Prisma-Discipline-Model vorhanden — nur name, teilerFaktor etc. existieren | Vor UI-Binding: Prisma-Schema durchsuchen um zu sehen welche Felder real vorhanden sind — keine Annahmen nach "logischem" Namen |
| 2026-03-17 | Action-Tests schlugen fehl weil neue Required-Felder in FormData hinzugekommen waren, alte Mocks wurden nicht aktualisiert | Tests nach Schema-Changes: alle FormData-Mocks durchsuchen und neue Required-Felder nachziehen — sonst `validation failed` durch unvollständige Test-Inputs |
| 2026-03-17 | Server-Action FormData-Transform: `"mixed"` Sentinel muss explizit auf `null` gesetzt werden statt Default-Wert zu nutzen | Beim Handling von nullable String-Enums in FormData: `.transform()` mit expliziter Fallunterscheidung nutzen, z.B. `(v) => (v && v !== "mixed" ? v : null)` |
| 2026-03-17 | Dashboard-Logik zur Trennung LEAGUE vs. non-LEAGUE-Wettbewerbe gehört in Page-Komponente, nicht in separate Hilfsfunktion | Type-Guards sollten als Inline-Logik in der Page bleiben wenn sie nur dort benötigt werden; dadurch bleibt die Trennung LEAGUE-spezifischer Routes ersichtlich |
| 2026-03-17 | Neue AuditLog-Ereignistypen (EVENT_SERIES_ENTERED, EVENT_SERIES_CORRECTED, EVENT_SERIES_DELETED) erforderten Parallel-Updates in types.ts, Enum-Definitionen, Labels, Kategorien und Detail-Formattern | Audit-Event-Typen zentralisiert in einer Datei pflegen; beim Hinzufügen müssen Labels, Kategorien und Formattern direkt mit dem Enum aktualisiert werden — Checkliste: Enum + Labels-Map + Categories-Map + Formatters-Switch |
| 2026-03-17 | PostgreSQL Unique-Constraint mit NULL-Werten: `@@unique([matchupId, participantId])` erlaubt mehrere Reihen mit NULL-matchupId — führte anfangs zu Verwirrung ob mehrere Saison-Serien pro Participant erlaubt sind | SQL-Standard: NULL ist nicht gleich NULL in Unique-Constraints. UNIQUE-Indexes schliessen mehrere NULL-Werte nicht aus — dies ist beabsichtigt für Saison/Event-Series. Liga-Series (mit matchupId) brauchen nicht-NULL-Enforcement über denselben Index. Dokumentieren: welche Entity-Kombinationen erlaubt sind, warum NULL-Distinctness erwünscht ist |
| 2026-03-17 | Saison-Series-Pattern mit `create` statt `upsert`: Mehrere Serien pro Participant pro Saison erlaubt (Liste, nicht Singleton) — nicht mit EVENT-Mode (eine Serie pro Participant) verwechseln | Saison-Wettbewerbe (`competition.type === "SEASON"`): immer `db.series.create()` ohne `findFirst`-Check. EVENT-Wettbewerbe: zuerst `findFirst`, dann upsert. Unterschied im Comment + Tests dokumentieren |
| 2026-03-17 | calculateSeasonStandings: bestRings, bestCorrectedTeiler, bestRingteiler sind unabhängige Metrik-Ränge — beste Ringe können aus einer anderen Serie kommen als bester Teiler | Aggregation über Serien-Liste mit unabhängigen `Math.max` / `Math.min` pro Metric. Nicht die beste Serie suchen und alle Metriken von dort nehmen — drei best-of-one-serie-Operationen, ergebnis-unabhängig voneinander |
| 2026-03-17 | Disziplin-Fallback in saveSeasonSeries: `formData.disciplineId ?? cp.disciplineId ?? competition.disciplineId` in 3 Ebenen, dann nochmal Fetch falls nicht cp.discipline vorhanden — ineffizient und verwirrend | Gemischte Saison-Wettbewerbe (formData.disciplineId nötig): zuerst validieren + transformieren, dann lokale Variable `resolvedDisciplineId` setzen. Fallback-Kette (3 Ebenen) ist OK, aber Discipline-Fetch separat halten statt mit cp-Lazy-Loading zu vermischen — zweiter Fetch nur bei Select von nicht-cp-Discipline |
| 2026-03-17 | Series-Page in `/competitions/[id]/series` branched auf `competition.type` (SEASON vs EVENT) anstatt separate Routes `/series` und `/ranking` | Routing mit Type-Branching (if/else am Seiten-Anfang) sparen doppelte Routes + Keep URL konsistent. Event → Rankings-Ansicht, Season → Serien-Erfassung auf gleicher URL-Basis. Non-LEAGUE-Competitions umleiten: `competition.type !== "LEAGUE" → /ranking statt /schedule` |
| 2026-03-17 | SeasonStandingsEntry mit `bestRings_rank: number | null` — null beim Unqualifiziert-Status (meetsMinSeries=false), nicht nur grayed-out oder 0 | Unqualifizierte Teilnehmer: `meetsMinSeries=false` + alle rank-Felder auf `null` setzen (nicht skipped werden). UI rendert null-Rangs als leerer Cell oder `—`, nicht als Nummer + Opacity. Semantisch wichtig für Rankings-Interpretation |
| 2026-03-17 | Participant.contact von `String` zu `String?` geändert (Prisma), aber ParticipantRowActions-Props nicht aktualisiert → TypeScript-Fehler wegen Type Mismatch (`string` erwartet, `string \| null` geliefert) | Schema-Feld-Typ-Änderungen (required → optional/nullable): type.ts + alle komponenten-Props durchsuchen die diesen Typ nutzen. Cascade-Update: ParticipantListItem/Detail-Types aktualisieren, dann alle Props von Komponenten die diese Types benutzen (ParticipantRowActions, Forms etc.) |
| 2026-03-17 | Dialog re-open bug: `setOpen(false)` in render body (auch mit `useRef`-Guard) schlägt fehl weil React render synchron neu-ausführen kann. Korrekt: `useEffect(() => { if (state?.success) setOpen(false) }, [state])` — triggert nur bei State-Änderung |
| 2026-03-17 | Sorting by rank fields schlägt für unqualifizierte Teilnehmer fehl — `calculateSeasonStandings` vergibt Ränge nur wem `minSeries` erfüllt ist. Sorting by `_rank` lässt unqualifiziert-Einträge (rank=null) alle gleich, dann alphabetisch fallthrough. Korrekt: Nach tatsächlichen Werten sortieren (`bestRings`, `bestCorrectedTeiler`, `bestRingteiler`) |
| 2026-03-17 | Component definiert inside render — ESLint-Regel `react-hooks/static-components` wird flagged. Die Komponente resettet State bei jedem render. Lösung: Component außerhalb verschieben, benötigte State als Props durchreichen |
| 2026-03-17 | `setState` in `useEffect` — ESLint-Regel `react-hooks/set-state-in-effect` warnt. Bei legitimen Fällen (Server-Action Response → Dialog close): `eslint-disable-next-line` verwenden |
| 2026-03-17 | SEASON vs EVENT routing — Dashboard muss Nicht-Ligen aufteilen in `activeEvents` und `activeSeasons`, dann entsprechende Queries/Komponenten nutzen (`getEventWithSeries` vs `getSeasonWithSeries`, `EventRankingTable` vs `SeasonStandingsTable`) |
| 2026-03-18 | HTML date input (`type="date"`) erwartet ISO-Format (YYYY-MM-DD), aber `formatDateOnly()` liefert Locale-Format — führt zu leeren/falschen Voreinstellungen | Für Edit-Dialoge mit date inputs: `sessionDateIso: s.sessionDate.toISOString().slice(0, 10)` als separates Property durchreichen. `formatDateOnly()` nur für Display, nie für Form `defaultValue` nutzen |
| 2026-03-18 | Transaktion mit `Prisma.$transaction()`: Variablen innerhalb des Callbacks sind in der äusseren Scope nicht sichtbar (z.B. `const teiler` innerhalb des Callbacks kann nicht nachher in auditLog verwendet werden) | Variablen die in der Transaktion UND danach verwendet werden, müssen AUSSERHALB deklariert werden. Transaktion modifiziert Wert, gibt ihn aber nicht zurück → `let teiler; const result = db.$transaction(async (tx) => { teiler = ...; return result; }); auditLog({ teiler })` |
| 2026-03-18 | HTML `<fieldset disabled>` ist das Standard-Pattern zum Deaktivieren ganzer Form-Sektionen — eleganter als einzelne Inputs manuell zu disablen | Ein `<fieldset>`-Container mit `disabled`-Attribut deaktiviert alle darin enthaltenen Inputs + Labels + Buttons gleichzeitig. Besonders für Regel-Sektionen in Wettbewerb-Formularen nützlich, wo Liga-Regelset zusammen mit `shotsPerSeries` gesperrt sein soll |
| 2026-03-18 | `_count.matchups` in getCompetitionById select einbeziehen statt separat in Edit-Page zu fetchen — spart einen zusätzlichen DB Round-Trip | Alle Aggregate (`_count`, `_sum` etc.) gehören in den Haupt-Query. Edit-Pages nur einmal DB holen, nicht einmal für Daten + einmal für aggregates. Aggregates im select kosten minimal, sparen aber den Datenbeschaffungs-Overhead |
| 2026-03-18 | shadcn Select: `<SelectItem value="">` verursacht Runtime-Error — leere Strings können nicht als gültiger Select-Wert fungieren | Sentinel-Wert `"none"` verwenden, dann `z.preprocess((v) => (v === "none" ? null : v), ...)` vor `z.enum().nullable()` setzen, um NULL vor Enum-Validierung zu mappen |
| 2026-03-18 | Zod `z.preprocess()` muss VOR `z.enum().nullable()` stehen, nicht `.transform()` danach — Transform läuft nur nach erfolgreicher Validierung | Preprocessing-Logik (String → NULL) gehört in `.preprocess()`, Typ-Umwandlungen in `.transform()`. Reihenfolge: preprocess → enum → nullable → transform |
| 2026-03-18 | Variablen für Transaktion + Audit-Logs müssen AUSSERHALB des `$transaction`-Callbacks deklariert werden — Scope-Sichtbarkeit des Callbacks deckt keine äusseren Zuweisungen | Beispiel: `let teiler; const result = tx(() => { teiler = ...; return result; }); auditLog({ teiler })` — deklariere teiler outside, modifiziere inside, nutze outside |
| 2026-03-18 | RINGS-Modi brauchen `bestRings` + `bestRingteiler` parallel in StandingRow — höchster Serienwert (nicht niedrigster Teiler) ist der korrekte Tiebreaker | Dual-Tracking für jede Metrik: `bestRings` aus höchster Serie, `bestRingteiler` für den Teiler dieser best-rings-Serie. Beide müssen unabhängig gemappt und im Select vorhanden sein |
| 2026-03-18 | Editierte Migration-Datei nach `prisma migrate dev` wurde re-applied — Drift-Error weil DB-State nicht mit Migration-History übereinstimmt | Wenn eine bereits angewendete Migration-Datei nachträglich geändert wird, schlägt `prisma migrate dev` mit Drift-Error fehl. Fix auf Dev-DB: `prisma migrate reset --force` (Datenverlust akzeptabel vor Launch). Warnung: Migrationen NIE nach erstem Apply editieren — neue Migration anlegen |
| 2026-03-18 | Nach `prisma migrate dev` (neue Migration) waren TypeScript-Fehler auf neuen Feldern — generierter Client kannte neue Felder noch nicht | `prisma migrate dev` führt NICHT automatisch `prisma generate` aus. Nach jeder neuen Migration explizit `npx prisma generate` aufrufen, bevor Typecheck oder Build läuft — sonst zeigt TypeScript Fehler auf validen neuen Feldern |
| 2026-03-18 | `finaleScoringMode` Enum bündelte mehrere Verhaltensweisen implizit — Primary-Kriterium, Tiebreaker und Teiler-Input-Flag waren nicht trennbar | Überladene Enums in explizite Felder aufteilen: `finalePrimary + finaleTiebreaker1 + finaleTiebreaker2` statt eines Alles-in-einem-Enum. Explizite Felder sind einzeln testbar, besser dokumentierbar und ohne Breaking-Change erweiterbar |
| 2026-03-18 | `shadcn Select` sendet Sentinel-String (`"none"`) für leere Auswahl — direkt an Zod-Enum übergeben führt zu Validierungsfehler | Muster für optionale Select-Felder mit Sentinel: `.preprocess((v) => (v === "none" \|\| !v ? null : v), z.enum([...]).nullable())`. Sentinel-Mapping gehört in `preprocess`, nicht in `transform` — `transform` läuft erst nach erfolgreicher Validierung |
| 2026-03-24 | Gast-Teilnehmer-Feature: `participantId` nullable zu machen hätte 20+ Relationen (kompetitionen, Spielpaarungen, Serien) ändern erfordert — stattdessen "silent record"-Pattern mit `isGuestRecord: true` flag, überall in Queries gefiltert | Optionale Relationen über mehrere Layer vermeiden: ein echten Record mit verstecktem Flag erstellen, alle List-Queries konsistent filtern (`where: { isGuestRecord: false }`). Spart Migrations- und Refactoring-Aufwand; bleibt semantisch sauber (Gast ist trotzdem ein Participant, nur intern markiert) |
| 2026-03-24 | Migration-File mit `IF NOT EXISTS` fix musste Recovery-Handler in `resolve-failed-migrations.mjs` bekommen — bereits in DB angewendete Spalte konnte nicht erneut erstellt werden | Wenn eine Migration auf dev-DB bereits teilweise/vollständig angewendet wurde bevor sie korrigiert wurde: Recovery-Handler schreiben der Safe-Idempotenz erzwingt (z.B. `IF NOT EXISTS` statt blankem `CREATE`). Nicht nur lokal `migrate reset --force` sondern auch für andere Entwickler dokumentieren/Code bereitstellen |
| 2026-03-24 | Guest-Display-Namen: Vorname-only vs. "Nachname, Vorname"-Pattern erfordert conditional Render-Logik mit `participant.isGuestRecord` in mehreren Komponenten (Tabellen, Karten, Ergebnis-Rows) | Display-Namen-Konvention nicht nur in einer Formatierungs-Funktion zentral, sondern Review aller Render-Orte: überall wo `participant.name` angezeigt wird checken ob `isGuestRecord` Property gekannt wird. Am sichersten: neue Felder sofort Komponenten-Props hinzufügen die sie nutzen, statt "später checken" |
| 2026-03-24 | react-pdf SchedulePdf: Matchup-Reihen (View ohne `wrap={false}`) können über Seitengrenzen hinweg splitten — Name auf einer Seite, Ergebnis auf der nächsten | PDF-Komponenten mit `<View>` immer `wrap={false}` setzen um Zeilen zusammenzuhalten. ReactPDF bricht längere Elemente standartmäßig um wenn sie nicht passen — explizites `wrap={false}` erzwingt Seitenumbruch statt Element zu splitten |
| 2026-03-24 | PlayoffBracket PAIR_GAP 32px zu klein: Mit 80px Kartenhöhe wirkte der Abstand zwischen AF-Paaren gequetscht und optisch unausgewogen | Spacing-Verhältnis bei gehäuften UI-Elementen überprüfen: `cardHeight × 0.5 = gap` (Faustregel). Für 80px Höhe mindestens 40–48px Gap für optisches Gleichgewicht; 32px war 40% statt 50% des Card-Abstands |
| 2026-03-24 | PlayoffsPdf mit Standard-Konstanten (SLOT_H=80, SLOT_W=176, PAIR_GAP=32) erzeugte 788pt breite Klammer > 769pt verfügbar auf A4-Querformat, überlauf + Abschneidung. PDF braucht eigene kompakte Konstanten | PDF-Rendering und Web-Rendering NIEMALS die gleichen Spacing-Konstanten teilen — A4 Querformat (769pt × 546pt) hat deutlich tightere Constraints als Bildschirm. PDF-only Module mit `// PDF-specific constants` kennzeichnen, kompakte Varianten testen: SLOT_H=48, SLOT_W=164, PAIR_GAP=12 für 722×420pt = sicher auf Seite |
| 2026-03-25 | EventRankingPdf: `W.disc` Zugriff auf Union-Typ `typeof W_MIXED \| typeof W_SINGLE` verursachte TS2769 auch mit `"in"`-Narrowing-Guard — react-pdf `<Text>` benötigt konkrete Typ-Constraints | react-pdf `<Text>`-Komponente mit Union-Types: Type-Narrowing (`if ("disc" in W) { W.disc }`) hilft nicht, weil react-pdf die Property-Signatur zur Compile-Zeit braucht. Lösung: Statt auf `W.disc` direkt mit `W_MIXED.disc` oder `W_SINGLE.disc` zugreifen, je nachdem welche Konstante tatsächlich verwendet wird — keine Indirektion über Union-Variable |
