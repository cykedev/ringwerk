# Lernlog – Liga-App

Wird nach jeder Nutzerkorrektur aktualisiert.
Format: Datum | Fehler | Regel die ihn verhindert

---

<!-- Zuletzt konsolidiert: 2026-03-26 -->

| 2026-03-27 | Vitest pickt Testdateien aus Git-Worktrees im Projektverzeichnis auf — Worktree-Tests laufen doppelt und schlagen fehl weil Mock-Imports anders auflösen | In `vitest.config.ts` `.claude/worktrees/**` explizit unter `exclude` eintragen. Gilt für alle Worktrees die innerhalb des Projektverzeichnisses liegen |
| 2026-03-27 | `docker compose -f /abs/path/docker-compose.yml` nutzt das Verzeichnis der Compose-Datei als Projekt-Verzeichnis für Volume-Mounts — Änderungen im Worktree sind im Container nicht sichtbar | Worktrees funktionieren in diesem Projekt nicht mit dem `/check`-Kommando. Stattdessen Branch direkt im Hauptverzeichnis auschecken (`git checkout <branch>`) und von dort arbeiten |
| 2026-03-27 | `docker compose --project-directory <worktree>` erstellt ein komplett neues Docker-Projekt mit eigenen Containern und läuft Migration neu — funktioniert nicht als Drop-in-Ersatz | `--project-directory` nur verwenden wenn ein eigener DB-Stack gewünscht ist. Für Check-Kommandos in diesem Projekt: Branch im Hauptverzeichnis auschecken | Neuer ScoringMode-Wert TARGET*OVER fehlte in allen lokalen Label-Maps (ranking/page.tsx, standings/page.tsx, EventRankingTable, EventTeamRankingTable, EventRankingPdf, SeasonStandingsPdf) — jede Datei pflegt eine eigene Record<string,string>-Map | Bei neuen ScoringMode-Werten: alle lokalen Label-Maps in pages, components und PDF-Dateien suchen (grep TARGET_UNDER) und parallel aktualisieren — sie werden nicht zentral verwaltet |
| 2026-03-26 | Neuer ScoringMode `TARGET_OVER` zum Prisma Enum hinzugefügt, aber Zod Validation Schema in `actions.ts` nicht aktualisiert — Form-Submit schlägt fehl mit "Ungültiger Wertungsmodus" | Bei neuen ScoringMode-Werten: das Zod Enum in `src/lib/competitions/actions.ts` (BaseSchema.scoringMode, Zeile ~28) ist eine manuelle Liste, nicht vom Prisma Schema abgeleitet — muss parallel aktualisiert werden. UI-Dropdown und Scoring-Engine können den Wert haben, aber Server-Validation schlägt fehl wenn Zod nicht synchronized ist |
| 2026-03-26 | Prisma Enum um neuen Wert erweitert, aber TypeScript kennt ihn nicht — `prisma generate` nicht ausgeführt | Bei Änderungen an Prisma Enums im Schema: immer `prisma generate` vor der Verwendung des neuen Wertes ausführen, damit der TypeScript Client regeneriert wird. Enum-Werte werden nicht automatisch aus der Schema-Datei gelesen |
| 2026-03-26 | Partielle Unique-Indizes in PostgreSQL ermöglichen Doppel-Einschreibung bei Team-Events, verhindern aber Duplikate bei Einzel — `WHERE eventTeamId IS NULL` vs. `WHERE eventTeamId IS NOT NULL` | Bei nullable FK-Feldern, die die Eindeutigkeitsbedingung kontrollieren, partielle Indizes statt globaler `@@unique`-Constraints verwenden |
| 2026-03-26 | `rankByScore` nutzte `participantId` als Identity-Key — Doppel-Enrollment (zwei CPs für denselben Teilnehmer) führte dazu, dass nur ein Eintrag in der Rangliste erschien | Bei Team-Events den `seriesId` als Identity-Key verwenden, nicht `participantId` — jede Serie ist eindeutig, auch wenn Teilnehmer mehrfach eingeschrieben sind |
| 2026-03-26 | Migration P3009: Enum `TeamScoring` bereits vorhanden → Migration als failed markiert obwohl sie teilweise durchgelaufen war | Migrations-SQL konsequent idempotent schreiben: `DO $$ BEGIN CREATE TYPE...; EXCEPTION WHEN duplicate_object THEN null; END $$;` + `IF NOT EXISTS` für alle DDL-Statements |
| 2026-03-26 | `unenrollParticipant` in `$transaction` gewrapped für Nicht-Team-Teilnehmer → Test erwartete keinen Transaction-Aufruf | `$transaction` nur wenn wirklich nötig (mehrere atomare Operationen); einfaches `db.competitionParticipant.delete` für unkomplizierte Einzellöschungen ohne Cleanup |
| 2026-03-26 | getEventWithSeries und getSeasonWithSeries filterten WITHDRAWN-Teilnehmer nicht — deren Serien/Einschreibungen wurden trotzdem in Ranglisten angezeigt | Bei Series/Participant-Queries für Ranglisten immer status: "ACTIVE" filtern — WITHDRAWN-Teilnehmer haben ihren CP-Status gesetzt, aber ihre Daten bleiben in der DB |
| 2026-03-25 | EventRankingPdf: `W.disc` Zugriff auf Union-Typ `typeof W_MIXED \| typeof W_SINGLE` verursachte TS2769 auch mit `"in"`-Narrowing-Guard — react-pdf `<Text>` benötigt konkrete Typ-Constraints | react-pdf `<Text>`-Komponente mit Union-Types: Type-Narrowing (`if ("disc" in W) { W.disc }`) hilft nicht, weil react-pdf die Property-Signatur zur Compile-Zeit braucht. Lösung: Statt auf `W.disc` direkt mit `W_MIXED.disc` oder `W_SINGLE.disc` zugreifen, je nachdem welche Konstante tatsächlich verwendet wird — keine Indirektion über Union-Variable |
| 2026-03-24 | Gast-Teilnehmer-Feature: `participantId` nullable zu machen hätte 20+ Relationen (kompetitionen, Spielpaarungen, Serien) ändern erfordert — stattdessen "silent record"-Pattern mit `isGuestRecord: true` flag, überall in Queries gefiltert | Optionale Relationen über mehrere Layer vermeiden: einen echten Record mit verstecktem Flag erstellen, alle List-Queries konsistent filtern (`where: { isGuestRecord: false }`). Spart Migrations- und Refactoring-Aufwand; bleibt semantisch sauber |
| 2026-03-24 | Migration-File mit `IF NOT EXISTS` fix musste Recovery-Handler in `resolve-failed-migrations.mjs` bekommen — bereits in DB angewendete Spalte konnte nicht erneut erstellt werden | Wenn eine Migration auf dev-DB bereits teilweise/vollständig angewendet wurde bevor sie korrigiert wurde: Recovery-Handler schreiben der Safe-Idempotenz erzwingt. Nicht nur lokal `migrate reset --force` sondern auch für andere Entwickler dokumentieren |
| 2026-03-24 | Guest-Display-Namen: Vorname-only vs. "Nachname, Vorname"-Pattern erfordert conditional Render-Logik mit `participant.isGuestRecord` in mehreren Komponenten (Tabellen, Karten, Ergebnis-Rows) | Display-Namen-Konvention nicht nur in einer Formatierungs-Funktion zentral, sondern Review aller Render-Orte: überall wo `participant.name` angezeigt wird checken ob `isGuestRecord` Property bekannt ist |
| 2026-03-24 | react-pdf SchedulePdf: Matchup-Reihen (View ohne `wrap={false}`) können über Seitengrenzen hinweg splitten — Name auf einer Seite, Ergebnis auf der nächsten | PDF-Komponenten mit `<View>` immer `wrap={false}` setzen um Zeilen zusammenzuhalten |
| 2026-03-24 | PlayoffBracket PAIR_GAP 32px zu klein: Mit 80px Kartenhöhe wirkte der Abstand zwischen AF-Paaren gequetscht und optisch unausgewogen | Spacing-Verhältnis bei gehäuften UI-Elementen überprüfen: `cardHeight × 0.5 = gap` (Faustregel) |
| 2026-03-24 | PlayoffsPdf mit Standard-Konstanten (SLOT_H=80, SLOT_W=176, PAIR_GAP=32) erzeugte 788pt breite Klammer > 769pt verfügbar auf A4-Querformat | PDF-Rendering und Web-Rendering NIEMALS die gleichen Spacing-Konstanten teilen — A4 Querformat (769pt × 546pt) hat deutlich tightere Constraints als Bildschirm |
| 2026-03-18 | RINGS-Modi brauchen `bestRings` + `bestRingteiler` parallel in StandingRow — höchster Serienwert (nicht niedrigster Teiler) ist der korrekte Tiebreaker | Dual-Tracking für jede Metrik: `bestRings` aus höchster Serie, `bestRingteiler` für den Teiler dieser best-rings-Serie — beide müssen unabhängig gemappt und im Select vorhanden sein |
| 2026-03-18 | `finaleScoringMode` Enum bündelte mehrere Verhaltensweisen implizit — Primary-Kriterium, Tiebreaker und Teiler-Input-Flag waren nicht trennbar | Überladene Enums in explizite Felder aufteilen: `finalePrimary + finaleTiebreaker1 + finaleTiebreaker2` statt eines Alles-in-einem-Enum |
| 2026-03-17 | `teilerFaktor` auf Discipline existierte seit Phase 1, wurde aber nie in Ringteiler-Berechnungen multipliziert — Faktor lag ungenutzt | Neue numerische Felder sofort in alle relevanten Rechner integrieren. Bei Phase 3 mussten results/actions.ts und playoffs/actions.ts `teilerFaktor` fetchen und durchreichen |
| 2026-03-17 | `calculateRingteiler` mit falscher Parameterreihenfolge führte zu stillen Bugs — korrekte Reihenfolge ist `(rings, teiler, teilerFaktor, maxRings)` | Numerische Kalkulationsfunktionen: Parameterreihenfolge in JSDoc + Tests dokumentieren; False-Positives durch Zahlen-Ähnlichkeit sind hard to debug |
| 2026-03-17 | Series-Model benötigte neues `competitionId String?`-Feld — EVENT-Series (matchupId=null) konnten sonst nicht zu Competition verlinkt werden | Neue relationale Entity-Felder vor Core-Logic-Implementierung definieren; danach erst die Queries + Actions anpassen |
| 2026-03-17 | Neue AuditLog-Ereignistypen (EVENT_SERIES_ENTERED, EVENT_SERIES_CORRECTED, EVENT_SERIES_DELETED) erforderten Parallel-Updates in types.ts, Enum-Definitionen, Labels, Kategorien und Detail-Formattern | Audit-Event-Typen zentralisiert in einer Datei pflegen; beim Hinzufügen: Enum + Labels-Map + Categories-Map + Formatters-Switch gleichzeitig aktualisieren |
| 2026-03-17 | PostgreSQL Unique-Constraint mit NULL-Werten: `@@unique([matchupId, participantId])` erlaubt mehrere Reihen mit NULL-matchupId — mehrere Saison-Serien pro Participant sind beabsichtigt | SQL-Standard: NULL ist nicht gleich NULL in Unique-Constraints. UNIQUE-Indexes schliessen mehrere NULL-Werte nicht aus — dies ist beabsichtigt für Saison/Event-Series vs. Liga-Series (mit matchupId) |
| 2026-03-17 | Saison-Series-Pattern mit `create` statt `upsert`: Mehrere Serien pro Participant pro Saison erlaubt (Liste, nicht Singleton) — nicht mit EVENT-Mode (eine Serie pro Participant) verwechseln | Saison-Wettbewerbe: immer `db.series.create()` ohne `findFirst`-Check. EVENT-Wettbewerbe: zuerst `findFirst`, dann upsert |
| 2026-03-26 | TARGET_OVER Penalty-Tier zeigte `+X` (wie TARGET_UNDER) — aber bei TARGET_OVER liegt der Wert \_unter* dem Ziel, also ist das Vorzeichen semantisch falsch | Bei TARGET_UNDER: Penalty = über Ziel → `+X`; bei TARGET_OVER: Penalty = unter Ziel → `-X`. formatScore/formatTeamScore muss die Modi getrennt behandeln, nicht zusammenfassen |
| 2026-03-17 | calculateSeasonStandings: bestRings, bestCorrectedTeiler, bestRingteiler sind unabhängige Metrik-Ränge — beste Ringe können aus einer anderen Serie kommen als bester Teiler | Aggregation mit unabhängigen `Math.max` / `Math.min` pro Metrik — nicht die "beste Serie" suchen und alle Metriken von dort nehmen |
| 2026-03-17 | SeasonStandingsEntry: unqualifizierte Teilnehmer (`meetsMinSeries=false`) brauchen `rank=null`, nicht 0 oder nur Opacity | `meetsMinSeries=false` + alle rank-Felder auf `null` — UI rendert null-Rangs als `—`, semantisch wichtig für Rankings-Interpretation |
| 2026-03-17 | Sorting by rank fields schlägt für unqualifizierte Teilnehmer fehl — null-Ränge sortieren alle gleich, dann alphabetisch | Nach tatsächlichen Werten sortieren (`bestRings`, `bestCorrectedTeiler`, `bestRingteiler`), nicht nach `_rank`-Feldern die null sein können |
| 2026-03-17 | SEASON vs EVENT routing — Dashboard muss Nicht-Ligen aufteilen in `activeEvents` und `activeSeasons` | Queries/Komponenten trennen: `getEventWithSeries` vs `getSeasonWithSeries`, `EventRankingTable` vs `SeasonStandingsTable` |
| 2026-03-16 | Beim Umbenennen von 57 Dateien über mehrere Layer: Page-Level-Props wurden nicht erfasst — sie referenzieren Component-Interface-Namen und wurden von Parallel-Layer-Agents übersehen | Grosse Refactorings mit Parallel-Agents: Pro Agent einen Layer — danach zusätzlich einen Pass für Page-Props. Props-Update läuft nach allen anderen Layer-Passes, nicht parallel |
| 2026-03-16 | Test-Suite fiel fehl wegen alten Git-Worktrees mit veralteten Import-Paths — Tests fanden die alten `.ts` noch auf der Disk obwohl der Code refaktoriert war | Vor `npm test` immer `rm -rf .claude/worktrees/*` ausführen — alte Session-Worktrees können Test-Imports vergiften |
| 2026-03-12 | Prisma-Migration musste manuell erstellt werden (Rename Column ist nicht-interaktiv) | Bei nicht-interaktiven Migrationen (Rename, Drop Column etc.) die Migration manuell schreiben, danach lokal `prisma migrate dev` aufrufen |
| 2026-03-11 | AuditLog-Einträge mit `{ matchupId }` als Detail waren nutzlos — kein Kontext ohne DB-Join | Audit-Details immer denormalisiert schreiben: Namen, Runden, Werte zum Zeitpunkt des Events direkt in `details` JSON — Details-JSON ist ein Snapshot, kein Verweis |
| 2026-03-11 | Protokoll-Link in jeden Sub-Page-Header eingebaut → Header zu voll, Nutzer-Feedback "zu breit" | Seltene Admin-Funktionen (Protokoll, Gefahrenzone) nicht in Navigation-Header — stattdessen in `...`-Dropdown (LeagueActions) oder eigene Sektion |
| 2026-03-11 | `PdfDownloadButton` hatte `size="sm"` mit sichtbarem Label — inkonsistent zu allen anderen Header-Buttons | Alle Buttons im Liga-Header: einheitlich `variant="outline" size="icon" className="h-9 w-9"` — Text nur in Hauptaktions-Buttons im Body |
| 2026-03-10 | Kaskadenlöschung ohne `onDelete: Cascade` im Prisma-Schema: `deleteMany` auf Parent-Tabelle schlägt fehl wegen FK-Constraints | Manuelle bottom-up-Reihenfolge in einer Transaktion: zuerst tiefste Kindtabellen (z.B. PlayoffDuelResults), dann aufwärts bis Root-Objekt (League) |
| 2026-03-10 | `preview_start` schlägt fehl wenn Docker Compose bereits manuell auf Port 3000 läuft | Docker immer über `preview_start` starten — nie manuell. Registrierung überlebt keine Session-Grenze: zu Sessionbeginn prüfen ob `preview_list` leer ist |
| 2026-03-09 | DB-Port 5432 bei parallelen Projekten belegt → Docker `up` schlägt fehl | In `docker-compose.dev.yml` den extern gemappten Port projektspezifisch wählen (ringwerk: 5433) |

## Abgeschlossen
